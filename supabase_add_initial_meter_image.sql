-- ==========================================================
-- เพิ่มคอลัมน์เก็บ "รูปมิเตอร์ตั้งต้น" ของแต่ละบ้าน
-- รันครั้งเดียวใน Supabase SQL Editor
-- ==========================================================

alter table houses add column if not exists initial_meter_image text;
